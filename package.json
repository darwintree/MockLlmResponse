{
  "name": "mock-llm-response-server",
  "version": "0.1.0",
  "private": true,
  "description": "Mock LLM response server supporting stream and tool calls",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js",
    "build": "tsc -p tsconfig.json",
    "check": "tsc --noEmit",
    "test": "pnpm build && node --test test/openai-sdk.test.mjs"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.13.10",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2",
    "openai": "^4.104.0"
  }
}
